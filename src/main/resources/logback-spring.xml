<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="archive" value="@logging.path@/archive"/>

    <!-- Load Spring Boot properties -->
    <springProperty scope="context" name="log-path" source="logging.path"/>
    <springProperty scope="context" name="springAppName" source="spring.application.name"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId:-}] [%X{spanId:-}]  [%-20thread] [${springAppName}] %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender with Rolling Policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>@logging.path@/${springAppName}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Hour-wise rolling -->
            <!-- <fileNamePattern>@logging.path@/${springAppName}.log.%d{yyyy-MM-dd_HH-mm}</fileNamePattern> -->
            <fileNamePattern>@logging.path@/${springAppName}.log.%d{yyyy-MM-dd_HH}</fileNamePattern>
            <maxHistory>20</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [${springAppName}] %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Custom Logger for Your Package -->
    <logger name="com.example.demo" level="DEBUG" additivity="false">
        <appender-ref ref="FILE" />
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
